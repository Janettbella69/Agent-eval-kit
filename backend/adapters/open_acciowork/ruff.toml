target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "UP", "B"]

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]

[lint.isort]
known-first-party = ["adapters"]
